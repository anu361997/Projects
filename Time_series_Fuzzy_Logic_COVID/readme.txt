The codes folder contains the codes for SEIR analysis for which youneed to import seir class into the google colaboratory.
The holts winter model is in R, thus it would require R-studio to run
The Fuzzy Time series model is a python notebook for which the dataset has been proc=vided which needs to be mount in the google colab. You need to have pyFTS and Simpsom module. If already installed kindly comment the first two lines of the code.
The traditional models that have been discussed are coded in the respective notebook. We need to have EoN module. if already present kindly comment the first line of the code.  
